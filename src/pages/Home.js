import React from "react";
import ToDo from "../components/ToDo";

const Home = () => {
    return (
        <section id="Home">
            <ToDo />
        </section>
    );
};

export default Home;
